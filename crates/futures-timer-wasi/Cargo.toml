[package]
name = "futures-timer"
version = "3.0.4"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "WASI-compatible drop-in for futures-timer (used as a [patch.crates-io] override)"
publish = false

[target.'cfg(all(target_arch = "wasm32", target_os = "wasi", target_env = "p2"))'.dependencies]
wstd   = "0.6"
wasip2 = "1.0"
